openai
streamlit
streamlit>=1.33.0
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9   # Postgres driver (safe to leave even if using SQLite)
openai>=1.12.0           # for LLM engine (only used if API key is provided)
pandas>=2.2.0            # optional: for analysis / future dashboards
